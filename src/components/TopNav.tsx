'use client';

export default function TopNav() {
  return (
    <header className="sticky top-0 z-50 flex items-center justify-between px-8 py-4 bg-white/80 backdrop-blur-md border-b border-[#e8e1da] min-h-[64px]">
      {/* Search field moved to page / table toolbars */}
    </header>
  );
}
